# research/ — Quant R&D workspace (Phase 9)

Owned by the Quant R&D runtime (Python 3.11). Structure per architecture §27.

- `factors/`     — factor definitions
- `models/`      — model definitions and training configs
- `strategies/`  — strategy compositions
- `baselines/`   — always-compared simple baselines (INV-16)
- `notebooks/`   — interactive exploration
