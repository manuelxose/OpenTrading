# data/schemas — dataset schemas (Parquet catalog, Phase 1)

Implemented in `adapters/market_data/storage.py`: `BAR_SCHEMA` and
`RAW_SCHEMA` (pyarrow), and the deterministic object-key layout documented in
`docs/architecture/PHASE1_DATA_PLATFORM.md`.

