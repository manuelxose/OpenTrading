# services/core-runtime — Core runtime (Python 3.12)

The core runtime (INV-13) is this repository's root package: `core/`, `engines/`,
`adapters/`, `apps/api`, `apps/worker`. Deployment assembly (Dockerfile, entrypoints)
lands with Phase 1.
